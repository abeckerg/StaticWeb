---
name: Workspace Copilot Instructions
description: "Guidance for Copilot behavior in this simple static task manager workspace"
applyTo: "**"
---

This workspace contains a small static web app in `docs/`.

- Suggest minimal, single-file edits when possible.
- Prefer accessible HTML and progressive enhancement (no build step required).
- When generating code, keep changes scoped to `docs/` and include small tests or usage notes.
